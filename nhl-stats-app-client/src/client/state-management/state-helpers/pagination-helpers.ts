import { PaginationData } from '../../../types/pagination';
import { PlayerStats } from '../../../types/player-stats';
import { AppState } from '../../../types/state';

// export const calculatePagination = (players: PlayerStats[], isFetch: boolean): PaginationData => {

//   const onFetchPaginationData: PaginationData = {}

// }
